/**
 * Bottom banner ad layout reserve (design px inside `#game-container`).
 * Slot sits under the Navbar (bottom-most); reclaim height when Remove Ads is active.
 */
export const BANNER_AD_RESERVE_HEIGHT_PX = 50;

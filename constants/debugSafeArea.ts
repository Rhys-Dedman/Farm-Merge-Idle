/** Screen px — UI top inset when fake notch preview is enabled from Dev Tools. */
export const FAKE_SAFE_AREA_TOP_PX = 54;

/** Dynamic Island pill (screen px), roughly iPhone 14 Pro proportions. */
export const FAKE_NOTCH_ISLAND_WIDTH_PX = 126;
export const FAKE_NOTCH_ISLAND_HEIGHT_PX = 37;
export const FAKE_NOTCH_ISLAND_TOP_PX = 11;
